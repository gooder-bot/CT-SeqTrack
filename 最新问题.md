# CT-SeqTrack 最新问题：代码、创新、梯度耦合与漂移恢复审查

## 2026-09-21：v31 三组正式完成，性能未通过；覆盖历史待运行状态

完整分析见 [v31 三组结果与优化报告](artifacts/ct_checks/reports/20260921_v31_mini_three_arm/REPORT.md)。代码与正式输出版本均为64ad056；三组60轮和58–60评测均完成，106轨迹/2285帧一致，逐帧重算与JSON一致。服务器只读，本轮没有修改模型、配置或正式输出。

| v31 | final60 S/P | late-3 S/P |
|---|---:|---:|
| B0 | 23.959519 / 27.834792 | 23.812546 / 28.955142 |
| Full-CfC | 22.491247 / 29.352297 | 24.213348 / 33.709701 |
| Full-GRU | 22.839169 / 31.844639 | 25.303793 / 37.146243 |

三组同分/缺标定回退已解决，B1真实训练与同帧耦合已接通，旧动作半径上限已移除；但两个Full的final Success均未超过B0。新版B0相对v30历史40.473742/47.840262下降16.5142/20.0055，不能宣布原方案性能验收成功。GRU是本次更好的工程后端，不构成连续时间结构收益证明。

本轮必须替代早期推测的新证据：

1. **当前帧身份判别先出问题。** 实际epoch60权重和所有102条非singleton轨迹首预测，3239个有效当前点中599个为GT前景；B0/CfC预测前景比例100%，GRU90.34%。11帧为空，100%的分母是有效点。训练场景、GT历史和临时batch统计下，当前帧也明显弱于历史hint可见帧。定位→分类概率通路的梯度尺度远大于0.1×CE，但方向混合，不能断言所有定位梯度都在鼓励背景。
2. **稀疏token和BN尾批是新增缺口。** 紧凑唯一点每8槽池化，40点仅5个有效局部token。epoch60最后四批为同轨迹单行递归，逐点BN仍更新；换58BN到60参数提升首预测mean IoU 4.79个百分点，却基本不修复all-FG，说明BN不是全部根因。
3. **模式形成不等于模式正确。** CfC/GRU实际获取GT的260帧里，仅97/116帧有成员纯度≥0.5模式；该定义仍不保证位置正确。真实短探针中目标点常保留到256池，却没有进入模式；三个最终候选还会挤到同一背景区域。只换质量排序的同状态oracle改善有限，应先修模式形成和收敛后去重。
4. **记忆写入与候选脱节。** Full动态FG真实纯度只有14.29%/15.22%；即便框IoU≥0.5，也有140/145、138/148次写入前景不纯。全池FPS允许与选中框无关的预测FG进入记忆；构造反例证明可放大远处假FG，但实测主要上游问题仍是分割。不得把“95%错写”误读为“95%框错误”。
5. **训练/部署状态分布仍不同。** 课程结束后47.24%训练端点是GT初始化窗口首步，最长递归8步，无独立锚点扰动；评估第11次及以后B0仅10.61/13.21。平滑错误框差仍可通过B1 pair规则，重定位后旧可信速度也可能残留。

唯一建议主线：保留联合四候选，以当前帧身份支持约束观测和状态；分类概率用于定位聚合的边停止梯度，共享特征仍联合学习；恢复anchor局部观测表示与真实有效点token；统一尾批BN处理并补连贯锚点扰动/较长递推；修收敛模式去重、候选一致记忆写入、可信运动资格。GRU先作主后端，CfC保留机制对照。具体修改顺序和文献采用边界见完整报告第6–7节，尚未实施这些建议。

本地SeqTrack有三次真实mini末轮50.99/59.96、31.68/31.34、28.00/26.48，不能选最高一次称稳定基线；完整nuScenes论文62.55/71.46更不能当mini分数。旧v30的“含前景模式约92%几何正确”仍是旧版证据，不适用于v31。

## 清理与修订的边界（2026-09-22）

本轮只做 v31 行为等价清理，上述算法修订仍未实施。9/21新增事实、数字和限制在本页完整保留；清理前完整文件位于 artifacts/ct_checks/20260922_v31_slimming/before/，旧正文通过 [历史索引](docs/HISTORY_EVIDENCE_INDEX.md) 查阅。不能将移除未执行旧分支表述为解决了上述掉分问题。

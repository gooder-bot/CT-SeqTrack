_base_: 30_formal_base.yaml
# 已有只读核实的 KITTI tracking 根目录；允许 --path 覆盖。
dataset: kitti_mf
version: kitti_tracking
path: /home/lishengjie/data/cxtrack/training
ct_coordinate_mode: sensor_relative
coordinate_mode: velodyne
kitti_frame_period: 0.1
default_time_step: 0.1
dynamics_fixed_delta_t: 0.1
train_split: train
val_split: val
test_split: test

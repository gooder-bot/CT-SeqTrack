_base_: 13_b2_v3_refiner.yaml

experiment_name: b2_v3_action_consistent_selective

# Evaluation-only after package_b2_v3_checkpoint.py injects a calibrated
# router.  Use --proposal-mode for the four unambiguous leaderboard modes.
proposal_inference_mode: obs_vs_all
router_v3_enabled_scale: 1.0
b2_v3_require_packaged_router: true
export_proposal_diagnostics: true
